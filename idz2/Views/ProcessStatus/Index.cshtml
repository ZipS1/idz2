@model IEnumerable<idz2.Models.ProcessStatus>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProcessStatusDescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ProcessStatusDescription)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ProcessStatusCode">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ProcessStatusCode">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ProcessStatusCode">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
